[build-system]
requires = ["setuptools>=42",
            "wheel",
            "pybind11~=2.6.1",
            "numpy>=1.7.1",
            "scipy>=0.9",
            "matplotlib>=3.3.3",
            "versioneer"]
build-backend = "setuptools.build_meta"

